# 설계 v1

이 저장소는 에이전트가 읽는 저장소 문서를 시간이 지나도 흔들리지 않게 쓰는 방법을 제시한다. 모든 권고는 실제 저장소의 수정 이력을 측정한 데이터로 뒷받침한다. 시대에 따라 바뀌는 관행은 갱신하고, 표본 기준과 측정 방법과 원칙은 유지한다.

## 1. 범위

- **대상 문서**: 소개, 구조 설명, 에이전트 지시, 기여 안내 네 역할의 저장소 문서
- **재는 것**: 안정성(얼마나 덜 흔들리는가)과 정확성(적힌 참조가 실재하는가)
- **재지 않는 것**: 문서가 에이전트의 작업 성공에 도움이 되는가. 이 질문은 별도 연구의 몫이며 참고 문헌으로만 연결한다.

## 2. 층 구조

위 층일수록 드물게 바뀌고, 아래 층은 위 층의 규칙을 따른다.

| 층 | 폴더 | 바뀌는 방식 |
|---|---|---|
| 표본 무결성 불변식 | `invariant.md` | 바꾸지 않는다 |
| 측정 모델 | `model/` | 바뀌면 버전을 올리고 결정 기록을 남긴다 |
| 원칙 | `principles/` | 여러 스냅샷에서 확인된 주장만 들어온다 |
| 관행 | `practices/` | 시대별. 조항마다 재검증 기한이 있다 |
| 결정 기록 | `decisions/` | 추가만 한다. 기존 기록은 새 기록으로 대체한다 |
| 스냅샷 | `snapshots/` | 추가만 한다. 한 번 쓰면 바꾸지 않는다 |

## 3. 표본 무결성 불변식

> 표본의 품질과 방향성 응집도가 기준에 미달하면, 그 표본에서 나온 결론은 채택하지 않는다.

측정 모델이 아무리 정확해도 표본이 흩어져 있거나 질이 낮으면 결론이 무너진다. 이 불변식은 두 관문으로 구현한다. 관문의 구체적인 기준값은 측정 모델의 매개변수이므로 모델 버전과 함께 바뀔 수 있다. 관문이 존재한다는 사실과 순서는 바뀌지 않는다.

### 관문 1: 품질 (입구, 저장소 단위)

표본에 들어오는 저장소마다 확인한다. 떨어진 저장소는 사유와 함께 스냅샷에 기록한다.

1. **모집단 소속**: 스냅샷이 선언한 모집단에 속한다. 성격이 다른 저장소를 한 표본에 섞지 않는다.
2. **활동성**: 관측 기간의 커밋 수가 기준 이상이고, 보관 상태가 아니다.
3. **문서 실재**: 역할 문서가 별칭을 따라간 뒤에도 실재하고, 빈 껍데기가 아니다.
4. **이력 연속성**: 관측 기간에 역할 문서가 이동하거나 이름이 바뀌어 이력이 끊기지 않았다.

### 관문 2: 응집도 (출구, 주장 단위)

모든 주장에 응집도 점수를 붙인다. 기준에 미달한 주장은 `proposed`에 머문다.

1. **층 일치**: 역할, 저장소 규모, 주 언어로 나눈 모든 층에서 효과의 방향이 같다. 층마다 최소 표본 수를 채운다.
2. **재표본 일치**: 표본을 무작위로 다시 뽑아도 방향이 유지되는 비율이 기준 이상이다.
3. **시점 일치**: 원칙으로 승격하려면 기준일이 6개월 이상 떨어진 스냅샷 2개 이상에서 같은 방향이다.

역할 **사이**의 비교(예: 구조 설명이 에이전트 지시보다 안정적이다)는 역할 층 일치 대신 규모와 언어 층 일치를 확인한다.

## 4. 측정 모델 v1

### 단위와 역할

- **단위**: (저장소, 역할, 기준일)
- **역할**: 소개, 구조 설명, 에이전트 지시, 기여 안내. 역할과 파일 이름의 대응은 시대별 관행이므로 `practices/file-names.md`에 둔다.
- **정본 판정**: 심링크, 같은 내용의 사본, `@파일` 한 줄 포인터를 따라가 정본 하나만 잰다.

### 지표

| 지표 | 정의 |
|---|---|
| 크기 | 정본의 줄 수 |
| 변경 횟수 | 관측 기간에 정본을 바꾼 병합 제외 커밋 수 |
| 상대 변경률 | 변경 횟수 ÷ 같은 기간 저장소 전체 병합 제외 커밋 수 |
| 증감 방향 | 관측 기간 추가 줄 ÷ (추가 줄 + 삭제 줄) |
| 참조 유효율 | 저장소 참조 가운데 기준일 트리에 실재하는 비율. 저장소 참조는 첫 경로 구간이 저장소 최상위에 있는 백틱 토큰이다 |

### 문서 특징

주장을 만드는 설명 변수다. 코드 블록 안과 본문 백틱 안을 모두 본다.

- 명령 줄 비율, 수치·버전 밀도, 코드 블록 비율, 링크 수, 제목 수, 모듈별 세부 절 비율

### 기간과 판정

- **관측 기간**: 기준일 이전 365일
- **안정 판정**: 같은 스냅샷, 같은 역할 안에서 상대 변경률이 하위 25%이고 증감 방향이 추가 쪽으로 치우치지 않은 문서. 절대값 기준은 쓰지 않는다.

### 재현성

- 원자료마다 측정한 커밋 SHA를 적는다.
- 저장소를 blobless clone하여 로컬 git으로 잰다. 호스팅 API 값은 쓰지 않는다.
- 한계(squash 병합 여부, 이력 단절)를 스냅샷 메타데이터에 적는다.

## 5. 조항 형식

원칙과 관행 조항은 파일 앞부분에 상태를 적는다.

```yaml
id: P-003
layer: practice            # principle | practice
status: adopted            # proposed | adopted | deprecated | superseded
as-of: 2027-03-24
review-by: 2027-09-24      # 관행만
evidence: [2027-03-24]     # 근거 스냅샷
coherence:                 # 관문 2 결과
  strata: {role: 4/4, size: 3/3, language: 3/3}
  resample: 0.94
references: [2, 5]
superseded-by: null
```

## 6. 흐름

1. **모집단 선언과 패널 구성**: 관문 1을 통과한 저장소로 패널을 만든다. 패널은 추가만 허용하고, 빠지는 저장소는 사유를 남긴다.
2. **측정**: 반기마다 스냅샷을 추가한다. 첫 공개 전에는 과거 기준일로 소급 측정해 시계열을 만든다.
3. **주장 도출**: 안정 문서와 불안정 문서의 특징 차이에서 주장을 만든다.
4. **관문 2**: 주장마다 응집도를 계산한다.
5. **채택**: 관문을 통과한 주장만 결정 기록과 함께 관행으로 올린다.
6. **승격과 폐기**: 시점 일치를 만족한 관행은 원칙으로 올린다. 재검증 기한까지 다시 확인되지 않은 관행은 `deprecated`로 바꾼다.

**권한**: 에이전트는 스냅샷 추가와 `proposed` 조항 작성까지만 한다. 채택, 승격, 폐기는 사람이 승인한 결정 기록으로만 한다.

## 7. 도구

Swift CLI 하나로 만든다. 이름은 기능이 드러나는 kebab-case 풀네임으로 짓는다.

| 명령 | 역할 |
|---|---|
| `collect --as-of <date>` | 패널을 측정해 새 스냅샷을 만든다 |
| `gate` | 관문 1(패널)과 관문 2(주장)를 계산한다 |
| `summarize <snapshot>` | 원자료에서 요약 표를 생성한다 |
| `check` | 이 저장소가 자기 규칙을 지키는지 검사한다 |
| `score <repo>` | 다른 저장소의 문서를 패널 기준으로 평가한다 (후순위) |

`check`가 검사하는 것:

- 스냅샷이 바뀌지 않았다 (해시 대조)
- 요약 표가 원자료에서 생성한 결과와 같다
- 관행과 원칙 조항에 관문 2 결과가 있고 기준을 넘는다
- 재검증 기한이 지난 관행이 없다
- 조항이 가리키는 스냅샷과 참고 문헌이 실재한다
- README와 AGENTS.md에 수치와 날짜가 직접 들어가지 않았다

## 8. 폴더 구조

```
stable-agent-documentation-guidebook/
├── README.md          소개, 층 구조, 가이드 요약, 인용 방법
├── AGENTS.md          이 저장소를 고치는 에이전트용 지도
├── DESIGN.md          이 문서
├── invariant.md       표본 무결성 불변식
├── model/             측정 모델 버전별 정의와 관문 기준값
├── principles/
├── practices/
├── decisions/
├── snapshots/         기준일별 원자료, 패널, 관문 결과, 메타데이터
├── REFERENCES.md
└── Package.swift, Sources/
```

## 9. 파일럿에서 얻은 교훈

`snapshots/2026-09-24-pilot/`의 탐색 측정에서 확인한 것이다. 이 스냅샷은 관문을 거치지 않았으므로 설계 판단의 입력으로만 쓴다.

- **역할 단위 측정이 필요하다.** 에이전트 지시 파일은 심링크와 포인터로 여러 이름에 겹쳐 있는 경우가 흔했다.
- **참조는 저장소 참조만 세야 한다.** 실행 시 경로와 예제 파일 이름을 참조로 세면 유효율이 크게 왜곡된다.
- **역할을 섞은 결론은 착시를 만든다.** 전체 표본에서 보인 "안정적인 문서는 명령이 적다"는 역할 층으로 나누자 절반의 층에서 방향이 뒤집혔다. 표본 무결성 불변식을 최상위에 둔 이유다.
- **명령 탐지는 본문 백틱까지 봐야 한다.**
- **역할별 표본 수가 부족했다.** 층 일치를 판정하려면 패널을 늘려야 한다.

## 10. 미결정

| 항목 | 추천 |
|---|---|
| 모집단 선언 | 활발한 오픈소스 개발 도구 저장소(컴파일러, 린터, 에디터, 빌드 도구, 에이전트 도구) |
| 관문 기준값 | 관측 기간 커밋 300개 이상, 층당 표본 8개 이상, 재표본 일치 0.9 이상 |
| 공개 여부 | public |
| 원격 저장소 소유 계정 | 미정 |
| 라이선스 | 문서 CC BY 4.0, 코드 MIT |
| 언어 | 영어 본문과 한국어 요약 |
| 측정 주기 | 반기 |
